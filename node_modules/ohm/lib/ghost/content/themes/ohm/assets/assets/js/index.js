'use strict';

(function () {
  'use strict';

  var React = require('react');
  var ReactDOM = require('react-dom');
  var io = require('socket.io-client');
  var LightBox = require('./ohm/LightBox.react.js');
  var GMap = require('./ohm/GMapper.react.js');
  var socket = io.connect();

  socket.on('connect', function (data) {
    ReactDOM.render(React.createElement(
      'div',
      null,
      React.createElement(LightBox, null),
      React.createElement(GMap, {
        scrollwheel: false,
        zoom: 9,
        mapName: 'ohm' }),
      React.createElement(
        'div',
        { id: 'general-error-wrapper' },
        React.createElement('div', { id: 'general-error' })
      ),
      React.createElement(
        'div',
        { id: 'loading' },
        React.createElement('div', { id: 'loading-indicator' })
      )
    ), main);
  });
})();
//# sourceMappingURL=index.js.map
